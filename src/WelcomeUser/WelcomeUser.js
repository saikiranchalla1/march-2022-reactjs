export default function WelcomeUser({username, notification}) {
    //const {username, notification} = props;

    return <div>Welcom {username}! You've got {notification} unread notifications.</div>
}
// Virtual DOM
